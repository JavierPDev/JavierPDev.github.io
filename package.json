{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Javier's portfolio.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "",
    "build": "gulp build.css && gulp build.html && gulp build.js && gulp build.img && gulp build.favicon",
    "sync": "gulp sync"
  },
  "author": "Javier Pena",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "latest",
    "gulp": "latest",
    "gulp-autoprefixer": "latest",
    "gulp-concat": "latest",
    "gulp-concat-css": "latest",
    "gulp-copy": "0.0.2",
    "gulp-htmlmin": "^3.0.0",
    "gulp-minify-css": "latest",
    "gulp-strip-line": "0.0.1",
    "gulp-uglify": "^2.0.0"
  },
  "dependencies": {
    "bootstrap": "^3.3.7",
    "jquery": "^3.1.0",
    "jquery-visible": "git+https://github.com/customd/jquery-visible.git"
  }
}
